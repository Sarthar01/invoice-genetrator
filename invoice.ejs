<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Invoice</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mx-auto py-8">
        <div class="flex justify-between items-center mb-8">
            <img src="/logo.png" alt="Company Logo" class="h-16">
            <div>
                <h1 class="text-2xl font-bold">Tax Invoice/Bill of Supply/Cash Memo</h1>
                <p>(Original for Recipient)</p>
            </div>
        </div>
        <div class="border p-8">
            <div class="mb-4">
                <p><strong>Sold By:</strong> <%= data.sellerName %></p>
                <p><%= data.sellerAddress %></p>
                <p><strong>GST Number:</strong> <%= data.gstNumber %></p>
            </div>
            <div class="mb-4">
                <p><strong>Billing Address:</strong></p>
                <p><%= data.billingAddress %></p>
            </div>
            <div class="mb-4">
                <p><strong>Shipping Address:</strong></p>
                <p><%= data.shippingAddress %></p>
            </div>
            <div class="mb-4">
                <p><strong>Order Number:</strong> <%= data.orderNumber %></p>
                <p><strong>Order Date:</strong> <%= data.orderDate %></p>
                <p><strong>Invoice Number:</strong> <%= data.invoiceNumber %></p>
                <p><strong>Invoice Date:</strong> <%= data.invoiceDate %></p>
            </div>
            <table class="min-w-full border-collapse border">
                <thead>
                    <tr>
                        <th class="border p-2">Sl No</th>
                        <th class="border p-2">Description</th>
                        <th class="border p-2">Unit Price</th>
                        <th class="border p-2">Quantity</th>
                        <th class="border p-2">Discount</th>
                        <th class="border p-2">Net Amount</th>
                        <th class="border p-2">Tax Rate</th>
                        <th class="border p-2">Tax Amount</th>
                        <th class="border p-2">Total Amount</th>
                    </tr>
                </thead>
                <tbody>
                    <% data.items.forEach((item, index) => { %>
                        <tr>
                            <td class="border p-2"><%= index + 1 %></td>
                            <td class="border p-2"><%= item.description %></td>
                            <td class="border p-2"><%= item.unitPrice.toFixed(2) %></td>
                            <td class="border p-2"><%= item.quantity %></td>
                            <td class="border p-2"><%= item.discount.toFixed(2) %></td>
                            <td class="border p-2"><%= item.netAmount.toFixed(2) %></td>
                            <td class="border p-2"><%= item.taxRate %>%</td>
                            <td class="border p-2"><%= item.taxAmount.toFixed(2) %></td>
                            <td class="border p-2"><%= item.totalAmount.toFixed(2) %></td>
                        </tr>
                    <% }); %>
                </tbody>
            </table>
            <div class="mt-4">
                <p><strong>Total Amount in Words:</strong> <%= data.totalAmountInWords %></p>
            </div>
            <div class="mt-4">
                <p>Whether tax is payable under reverse charge - No</p>
            </div>
            <div class="mt-8 flex justify-between items-center">
                <p>For Varasiddhi Silk Exports:</p>
                <img src="/signature.png" alt="Signature" class="h-20">
            </div>
            <div class="mt-2">
                <p>Authorized Signatory</p>
            </div>
        </div>
    </div>
</body>
</html>
